package logic;

public class FormulaProvider {

    public String formulaGenerator(int numberOfVariableTerms){
        //((t1+c1)*t2)/(t3 mod c2)
        //fgs is formula generator simple
        //this could be (t1+c1), ((t1+c1) * fgs), ((t1+c1)*t2), ((t1+c1)*t2) / fg2,
        //fg2 = t3, (t3 mod fgs) ish
        return "";
    }
}
